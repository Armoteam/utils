{
	"packages": ["packages/*"],
	"version": "independent",
	"command": {
		"version": {
			"conventionalCommits": true,
			"createRelease": "github",
			"ignoreChanges": ["**/*.md"],
			"message": "chore(release): publish [skip ci]"
		}
	}
}
